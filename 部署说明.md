# 水泥销售记账 App 部署与使用说明

## 一、项目文件

```
cement-ledger/
├── index.html              # 主程序（HTML + CSS + JS）
├── manifest.json           # PWA 配置文件
├── README.md               # 仓库说明
├── 部署说明.md             # 本文件
└── icons/                  # App 图标（蓝色背景白色"封"字）
    ├── icon-192.png
    ├── icon-256.png
    ├── icon-512.png
    ├── icon-1024.png
    ├── apple-touch-icon.png
    └── favicon-32.png
```

## 二、本地使用

直接用手机或电脑浏览器打开 `index.html` 即可使用。首次打开会预置截图中常见的车号和出货人：

- 车号：2C961、861、43121、5586、543PG
- 出货人：大宁、安平、铁杆、立永、王峰、周伟

## 三、上传 GitHub 并开启 GitHub Pages

### 步骤 1：注册 / 登录 GitHub
打开 <https://github.com>，登录账号。

### 步骤 2：新建仓库
1. 点击右上角 **+** → **New repository**。
2. 仓库名填 `cement-ledger`（可自定）。
3. 选择 **Public**（Public 才能免费使用 GitHub Pages）。
4. 点击 **Create repository**。

### 步骤 3：上传所有文件
1. 进入刚创建的仓库主页。
2. 点击 **Add file** → **Upload files**。
3. 将 `index.html`、`manifest.json`、`README.md`、`部署说明.md` 和 `icons/` 文件夹**全部**拖入上传区域。
4. 点击下方 **Commit changes**。

> ⚠️ 必须上传 `icons/` 文件夹，否则添加到手机桌面时无法显示"封"字图标。

### 步骤 4：开启 GitHub Pages
1. 仓库页点击 **Settings**。
2. 左侧菜单选择 **Pages**。
3. **Branch** 选择 `main`（或 `master`），文件夹选择 `/(root)`，点击 **Save**。
4. 等待 1-3 分钟，页面上方会出现绿色提示：
   > Your site is live at `https://你的用户名.github.io/cement-ledger/`
5. 复制该链接，用手机浏览器打开即可使用。

## 四、添加到手机桌面当 App 使用

### iPhone（Safari）
1. 用手机 Safari 打开 GitHub Pages 链接。
2. 点击底部分享按钮 **⇧**。
3. 选择 **添加到主屏幕**。
4. 点击右上角 **添加**。
5. 桌面会出现蓝色"封"字图标，点击即可全屏使用。

### 安卓手机（Chrome）
1. 用手机 Chrome 打开 GitHub Pages 链接。
2. 点击右上角菜单 **⋮** → **添加到主屏幕**（或 **安装应用**）。
3. 按提示确认，桌面会生成蓝色"封"字图标。

## 五、数据安全提示

- 所有数据保存在当前浏览器中，清除浏览器数据会导致记录丢失。
- 可在"设置"页点击 **导出Excel** 备份数据，保存到手机或电脑。
- 如需换手机/浏览器，可手动导入备份（后续版本可扩展导入功能）。

## 六、功能摘要

| 模块 | 说明 |
|------|------|
| 单据列表 | Excel 风格表格查看、筛选、搜索、编辑、删除销售单据 |
| 新增单据 | 填写日期、车号、吨位、货物类型、出货人、单价，自动计算总金额 |
| 数据统计 | 总吨位、总销售额、已付/未付金额、按客户统计、付款状态明细查询 |
| 设置 | 管理出货人和车号，导出 Excel 备份 |
